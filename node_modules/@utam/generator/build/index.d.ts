import { GeneratorConfigOptions } from '@utam/types';
import { GenerationOutput } from './types';
export { createSingleOutputDirectory, writeOutputJSON } from './writer';
export { scan } from './scanner';
export { DEFAULT_INPUT_FILE_MASKS } from './utils/constants';
/**
 * Merge default options with override from config
 * @param options partial options
 * @returns resolved configuration
 */
export declare function resolveConfig(options: Partial<GeneratorConfigOptions>): GeneratorConfigOptions;
/**
 * Generate UTAM JSON from HTML file
 * Can return more than one output if option to generate content of custom tags is on.
 * @param htmlFilePath absolute path to HTML file
 * @param config generator config
 * @returns generated objects
 */
export declare function generateFromPath(htmlFilePath: string, config: GeneratorConfigOptions): GenerationOutput[];
//# sourceMappingURL=index.d.ts.map